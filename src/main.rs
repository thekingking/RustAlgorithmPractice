use std::collections::HashMap;
fn main() {
    println!("hello, world");
}

#[warn(dead_code)]
struct Solution;

impl Solution {
    pub fn number_of_pairs(mut nums1: Vec<i32>, mut nums2: Vec<i32>, k: i32) -> i64 {
    }
}